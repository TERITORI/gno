# gno lint: test file error

! gno lint .

cmp stdout stdout.golden
cmp stderr stderr.golden

-- gno.mod --
module gno.land/r/linterror

-- gh.gno --
package linterrors

func lint() {
	for {
		_ example
}

-- gh2.gno --
package linterror

func here() {
	for {
		_ example
}


-- stdout.golden --
-- stderr.golden --
i_have_error.gno:5: expected ';', found example (code=2).
i_have_error.gno:7: expected '}', found 'EOF' (code=2).